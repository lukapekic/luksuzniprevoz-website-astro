# Repository Documentation

This directory contains **operational and governance reference documentation** for the Luxury Transportation Astro repository.

It does not define the page design or replace the repository's primary authorities.

## Authority hierarchy

When documents disagree, use this order:

1. `AGENTS.md` — technical/project authority and mandatory agent workflow.
2. `DESIGN.md` — visual authority and approved design-system direction.
3. `site/luksuzni-prevoz/foundation.config.ts` — production-site configuration, including the single active-theme selector (`activeThemeVersion`).
4. Active theme source and generated theme output — machine implementation of the configured theme.
5. Locked page blueprints and shared design contracts under `site/luksuzni-prevoz/src/docs/` — page/section structure and responsive intent.
6. `.skills/` — task-specific procedures for implementation, review, accessibility, responsive work, SEO, and design governance.
7. This `docs/` directory — operational guidance, checklists, content workflow, scale assumptions, deployment notes, and waiver records.

A root doc must never override a locked blueprint, `DESIGN.md`, the active theme, or `AGENTS.md`.

## Root docs vs site docs

- `docs/` — repository operations and governance.
- `site/luksuzni-prevoz/src/docs/` — production page/component blueprints, wireframes, and structural contracts.

Do not move page design rules into root operational docs.

## Active documents

| File | Purpose |
|---|---|
| `a11y-manual-checklist.md` | Manual WCAG 2.2 AA release checks not fully covered by automation. |
| `content-authoring.md` | Editorial content, page-archetype, localization, SEO, image, and data-separation workflow. |
| `deployment.md` | Static production build/deploy requirements, headers, redirects, cache, forms, and post-deploy checks. |
| `exceptions.md` | Canonical FND-rule waiver registry parsed by `pnpm parse-waivers`. |
| `rule-traceability.md` | Generated FND rule-to-enforcer matrix. Never hand-edit. |
| `scale-envelope.md` | Architectural scale assumptions and signals that require a deliberate architecture change. |

## Generated documentation

`rule-traceability.md` is generated by:

```bash
pnpm traceability
```

Validate it with:

```bash
pnpm traceability --check
```

Do not edit it manually.

## Design/theme changes

The production site's active theme is selected **only** by:

```text
site/luksuzni-prevoz/foundation.config.ts
→ activeThemeVersion
```

Shared foundation code must not provide an active-theme fallback. A missing or invalid configured theme is an error.

After a theme-system change, follow the Theme Ownership and Upgrade Protocol in `AGENTS.md` and run the applicable theme and design-governance gates.

Blueprints and wireframes should remain theme-independent by referencing semantic tokens rather than literal colors, spacing, radii, or breakpoints.

## UI/design agent workflow

For production UI work, follow `AGENTS.md` and `.skills/design-governance.md`.

Minimum workflow:

```bash
pnpm design:context <target>
# implement
pnpm design:detect <target>
pnpm check
```

For completion-sensitive work, use the stricter repository gate when available:

```bash
pnpm design:guard
```

Harness-specific hooks are fast feedback only; the repository commands remain the canonical enforcement layer.

## Removed legacy docs

The following old template-era documents are intentionally not part of the active docs set:

- `init-checklist.md`
- `optional-vrt.md`
- `spec-amendments.md`

Do not restore them unless a current repository requirement explicitly needs them. Historical template onboarding and the old VRT reclassification are no longer active project authorities.
