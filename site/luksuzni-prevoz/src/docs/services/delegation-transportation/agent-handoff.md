# Agent Handoff — Delegation Transportation v1

Implement the locked Delegation Transportation page without redesigning it.

Before editing:

```text
read AGENTS.md
read DESIGN.md
read current shared service contracts
read this complete packet
load required skills
run design context
fill compliance matrix
verify every locked image/logo path
```

Then:

```text
1. Merge Delegation UI additions into SR/EN/RU dictionaries.
2. Regenerate current repository i18n/types.
3. Replace the three Delegation scaffold files with supplied content.
4. Run repository digest sync; never hand-author EN/RU digest.
5. Keep content in-review/noindex and route scaffold.
6. Create DelegationTransportationPage.
7. Create DelegationMovementSequence.
8. Create DelegationInstitutionalProof.
9. Reuse ServiceHero full-bleed with v-class-embassy-entrance.webp.
10. Reuse ServiceOverview numbered-divider-facts.
11. Compose Audience, Mixed Fleet, image-free contained Discretion and Briefing from shared primitives.
12. Read the exact three approved institutional clients through `getApprovedClientsFor("delegationTransportation")`, resolve their media through `clientLogoMedia`, assert locked order, and render unfiltered marks in individual light boxes on a dark section.
13. Reuse VehicleRecommendations with exact S/E/V order.
14. Reuse ServiceStandards numbered-matrix.
15. Resolve FAQ operational tokens after canonical assertions.
16. Keep securityService=false explicit in FAQ.
17. Keep NDA wording request-based and written-agreement-only.
18. Reuse FinalCTA with s-class-interior-1.webp.
19. Add one Delegation mapping to existing ContentPageRenderer.
20. Run responsive, accessibility, design and technical review.
21. Run every repository gate.
22. Publish all locales together only after acceptance is fully green.
```

Do not invent data, prices, routes or assets. Do not modify Corporate page-local panels. Do not reuse `BusinessClientProof`. Do not add political/security claims. Do not publish early.

Completion report MUST contain actual commands/results and remaining blockers.
