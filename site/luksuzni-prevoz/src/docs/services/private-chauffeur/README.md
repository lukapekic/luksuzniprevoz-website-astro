# Private Chauffeur v3 — Handoff Packet

This packet replaces the outdated Private Chauffeur page planning files.

Files:

```text
blueprint.md
wireframe.html
implementation.md
acceptance.md
```

Recommended repository destination:

```text
site/luksuzni-prevoz/src/docs/services/private-chauffeur/
```

The existing older files with the same names should be replaced only after the owner approves this v3 packet as the new locked authority.

## Agent instruction

The coding agent MUST treat:

```text
blueprint.md
```

as locked page-specific authority.

The agent MUST NOT reinterpret the section order, image roles, CTA hierarchy, data ownership, responsive topology, or component budget.

Implementation begins only after:

```text
AGENTS.md
DESIGN.md
blueprint.md
wireframe.html
implementation.md
acceptance.md
required .skills
```

are loaded and the blueprint compliance matrix is created.

## V3 content package

This packet also contains:

```text
content/
  private-chauffeur.sr.md
  private-chauffeur.en.md
  private-chauffeur.ru.md

ui-additions/
  sr.json
  en.json
  ru.json

content-contract.md
content-handoff.md
```

The three content files are complete reviewed v3 copy but deliberately remain
`status: in-review` + `noindex: true` until the implementation passes acceptance.

EN/RU use the Serbian source digest generated by
`pnpm content:sync-digests site/luksuzni-prevoz`; no digest literal in this
handoff overrides generator output.

The UI JSON files are merge fragments and MUST be merged into the existing
`src/content/ui/*.json` dictionaries, not used as replacements.

## Production integration status

The v3 package is integrated. Production content is `published`, reviewed and
indexable in Serbian, English and Russian; the route is published; shared media
lives under `src/assets/shared/chauffeur-service/`; and the page-specific
implementation evidence is recorded in `compliance-matrix.md`.

The source files retained inside this handoff packet preserve their original
pre-publication lifecycle metadata for provenance. They are not production
content and must not replace the published entries without rerunning the
content lifecycle workflow.
