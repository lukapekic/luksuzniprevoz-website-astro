# Agent Handoff — Corporate Transportation v1

Implement the locked Corporate Transportation page without redesigning it.

Before editing:

```text
read AGENTS.md
read DESIGN.md
read blueprint.md
read implementation.md
read acceptance.md
read content-contract.md
read asset-contract.md
read wireframe.html
load required .skills
run design context
create compliance matrix
```

Then:

```text
replace the three Corporate scaffold content files with supplied v1 content
merge UI fragments into existing locale dictionaries
keep content in-review/noindex and route scaffold during implementation
implement exact locked order
reuse full-bleed ServiceHero
reuse ServiceOverview
create only CorporateEngagementPanel and CorporateCoordinationPanel plus page renderer
compose Audience Rail and Working Day from primitives
reuse VehicleRecommendations
reuse ServiceStandards numbered-matrix
resolve FAQ operational tokens only after canonical assertions
reuse FinalCTA with booking + quote + the caller-owned locked interior image
add mapping to existing ContentPageRenderer
run every repository gate
publish only after all three locked shared images resolve and complete acceptance passes
```

The Serbian source digest is generator-owned. After installing or changing the
Serbian source, run `pnpm content:sync-digests site/luksuzni-prevoz` and re-review
EN/RU when the source copy changed.
