/** Three-tier public footer: utility, route-derived navigation, and bottom metadata. */
import { getPath } from "@astro-foundation/core/i18n";
import { routes } from "../../data/routes.ts";
import { navigation, getNavLabel } from "../../data/navigation.ts";
import { business } from "../../data/business.ts";
import { contact, isVerified } from "../../data/contact.ts";
import { t } from "../../lib/i18n.ts";
import BrandLockup from "../../foundation/ui/BrandLockup.astro";
import LanguageSwitcher from "../../foundation/ui/LanguageSwitcher.astro";
import type { LocaleCode, RouteKey } from "@astro-foundation/core";
import { defaultLocale } from "../../data/locales.ts";

interface Props {
  currentRouteKey: RouteKey;
  currentLocale: LocaleCode;
}
const { currentRouteKey, currentLocale } = Astro.props;
const href = (key: RouteKey): string => getPath(key, currentLocale, routes, defaultLocale);
const phone = isVerified(contact.phone) ? contact.phone.value : null;
const email = isVerified(contact.email) ? contact.email.value : null;
const location = business.headquarters.verificationStatus === "verified"
  ? `${t("location.belgrade", currentLocale)} · ${t("location.serbia", currentLocale)}`
  : null;
const address = contact.office.verificationStatus === "verified" ? contact.office.address : null;
const hours = `${t("footer.hoursDays", currentLocale)} · ${business.supportModel.officeHours}`;
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="site-footer__shell">
    <div class="site-footer__tier site-footer__utility">
      <BrandLockup locale={currentLocale} href={href("home")} variant="full" />
      <div class="site-footer__channels">
        {phone && <a class="site-footer__contact" href={`tel:${phone}`}>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M22 16.9v3a2 2 0 0 1-2.2 2A19.8 19.8 0 0 1 3.1 5.2 2 2 0 0 1 5.1 3h3a2 2 0 0 1 2 1.7l.4 2.8a2 2 0 0 1-.6 1.8l-1.4 1.4a16 16 0 0 0 4.8 4.8l1.4-1.4a2 2 0 0 1 1.8-.6l2.8.4a2 2 0 0 1 1.7 2Z"/></svg>
          <span>{phone}</span>
        </a>}
        {email && <a class="site-footer__contact" href={`mailto:${email}`}>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m2 6 10 7 10-7"/></svg>
          <span>{email}</span>
        </a>}
      </div>
      <p class="site-footer__hours">{hours}</p>
    </div>

    <nav class="site-footer__tier site-footer__nav" aria-label={t("nav.footer", currentLocale)}>
      {navigation.footer.map((group) => (
        <div class="site-footer__group">
          <h2 class="site-footer__heading">{getNavLabel(group.labelKey, currentLocale)}</h2>
          <ul role="list">
            {group.children.map((item) => (
              <li><a href={href(item.routeKey)} aria-current={item.routeKey === currentRouteKey ? "page" : undefined} data-astro-prefetch>{getNavLabel(item.routeKey, currentLocale)}</a></li>
            ))}
          </ul>
        </div>
      ))}
    </nav>

    <div class="site-footer__tier site-footer__bottom">
      <p>© {year} {business.publicBrand} · {t("footer.rights", currentLocale)}</p>
      <div class="site-footer__place">
        {address && <span>{address}</span>}
        {location && <span>{location}</span>}
      </div>
      <LanguageSwitcher currentLocale={currentLocale} currentRouteKey={currentRouteKey} presentation="inline" idPrefix="footer-lang" />
    </div>
  </div>
</footer>

<style>
  .site-footer { border-block-start: 1px solid var(--color-divider); background: var(--color-surface); color: var(--color-text-primary); }
  .site-footer__shell { inline-size: min(calc(100% - 2 * var(--gutter-page)), var(--container-main)); margin-inline: auto; }
  .site-footer__tier { padding-block: var(--space-8); }
  .site-footer__tier + .site-footer__tier { border-block-start: 1px solid var(--color-divider); }
  .site-footer__utility { display: grid; gap: var(--space-4); min-inline-size: 0; }
  .site-footer__channels { display: flex; flex-wrap: wrap; align-items: center; gap: var(--space-2) var(--space-6); min-inline-size: 0; }
  .site-footer__contact { display: inline-flex; align-items: center; gap: var(--space-2); min-block-size: 2.75rem; color: var(--color-text-primary); font-family: var(--font-body); font-size: var(--text-sm); text-decoration: none; overflow-wrap: anywhere; }
  .site-footer__contact svg { inline-size: var(--space-4); block-size: var(--space-4); flex: none; color: var(--color-accent); }
  .site-footer__hours { margin: 0; font-family: var(--font-body); font-size: var(--text-sm); color: var(--color-text-muted); }
  .site-footer__nav { display: grid; gap: var(--space-6); }
  .site-footer__group { min-inline-size: 0; }
  .site-footer__heading { margin: 0 0 var(--space-3); font-family: var(--font-body); font-size: var(--text-sm); font-weight: var(--font-weight-semibold); color: var(--color-accent); }
  .site-footer__group ul { display: grid; gap: var(--space-1); margin: 0; padding: 0; list-style: none; }
  .site-footer__group a { display: flex; align-items: center; min-block-size: 2.75rem; color: var(--color-text-primary); font-family: var(--font-body); font-size: var(--text-sm); line-height: var(--line-height-body); text-decoration: none; }
  .site-footer__group a[aria-current="page"] { color: var(--color-accent); text-decoration: underline; text-underline-offset: .2em; }
  .site-footer a:hover { color: var(--color-accent-hover); }
  .site-footer a:focus-visible { outline: 2px solid var(--color-focus-dark); outline-offset: 2px; }
  .site-footer__bottom { display: grid; gap: var(--space-3); align-items: center; font-family: var(--font-body); font-size: var(--text-xs); color: var(--color-text-muted); }
  .site-footer__bottom p { margin: 0; }
  .site-footer__place { display: flex; flex-wrap: wrap; gap: var(--space-1) var(--space-3); min-inline-size: 0; overflow-wrap: anywhere; }
  @media (min-width: 48rem) {
    .site-footer__utility { grid-template-columns: 1fr 1fr; align-items: center; }
    .site-footer__hours { grid-column: 2; }
    .site-footer__nav { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .site-footer__bottom { grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); }
  }
  @media (min-width: 80rem) {
    .site-footer__utility { grid-template-columns: minmax(0, 1fr) auto auto; }
    .site-footer__hours { grid-column: auto; }
    .site-footer__nav { grid-template-columns: repeat(4, minmax(0, 1fr)); }
    .site-footer__bottom { grid-template-columns: auto minmax(0, 1fr) auto; }
    .site-footer__place { justify-content: end; }
  }
</style>
