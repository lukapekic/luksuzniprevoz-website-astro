---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";
import Section from "../../../foundation/ui/Section.astro";
import PageContainer from "../../../foundation/ui/PageContainer.astro";
import SectionHeading from "../../../foundation/ui/SectionHeading.astro";

interface Group {
  title: string;
  text: string;
}

interface Props {
  heading: string;
  intro?: string;
  body: string;
  label: string;
  groups: readonly [Group, Group, Group];
  image: ImageMetadata;
}

const { heading, intro, body, label, groups, image } = Astro.props;
---

<Section spacing="feature" surface="open-dark" aria-labelledby="wedding-guests-heading">
  <PageContainer>
    <div class="guest-layout">
      <div class="guest-content">
        <p class="section-label"><span aria-hidden="true"></span>{label}</p>
        <SectionHeading id="wedding-guests-heading" {heading} description={intro} />
        <p class="body-copy">{body}</p>

        <ol class="guest-groups">
          {groups.map((group, index) => (
            <li>
              <span aria-hidden="true">{String(index + 1).padStart(2, "0")}</span>
              <div>
                <h3>{group.title}</h3>
                <p>{group.text}</p>
              </div>
            </li>
          ))}
        </ol>
      </div>

      <figure class="guest-media">
        <Image
          src={image}
          alt=""
          role="presentation"
          loading="lazy"
          widths={[400, 640, 800, 1000]}
          sizes="(min-width: 64rem) 38rem, (min-width: 48rem) 42rem, calc(100vw - 3rem)"
        />
      </figure>
    </div>
  </PageContainer>
</Section>

<style>
  .guest-layout {
    display: grid;
    gap: var(--space-8);
    align-items: center;
  }

  .guest-content {
    min-inline-size: 0;
  }

  .section-label {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-block-end: var(--space-5);
    font-family: var(--font-body);
    font-size: var(--text-ui);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-muted);
  }

  .section-label span {
    inline-size: var(--space-8);
    block-size: 1px;
    background: var(--color-accent);
  }

  .body-copy {
    max-inline-size: var(--measure-body);
    margin-block: var(--space-5) 0;
    font-family: var(--font-body);
    line-height: var(--line-height-body);
    color: var(--color-text-muted);
  }

  .guest-groups {
    margin: var(--space-8) 0 0;
    padding: 0;
    border-block-start: 1px solid var(--color-divider);
    list-style: none;
  }

  .guest-groups li {
    display: grid;
    grid-template-columns: var(--space-10) minmax(0, 1fr);
    gap: var(--space-4);
    padding-block: var(--space-5);
    border-block-end: 1px solid var(--color-divider);
  }

  .guest-groups li > span {
    font-family: var(--font-body);
    font-size: var(--text-caption);
    font-weight: var(--font-weight-semibold);
    color: var(--color-accent);
  }

  h3 {
    font-family: var(--font-heading);
    font-size: var(--text-xl);
    font-weight: var(--font-weight-semibold);
    line-height: var(--line-height-heading);
  }

  .guest-groups p {
    margin-block: var(--space-2) 0;
    overflow-wrap: anywhere;
    font-family: var(--font-body);
    font-size: var(--text-sm);
    line-height: var(--line-height-body);
    color: var(--color-text-muted);
  }

  .guest-media {
    margin: 0;
    aspect-ratio: 4 / 3;
    overflow: hidden;
    border-radius: var(--radius-card);
    background: var(--color-surface-elevated);
  }

  .guest-media :global(img) {
    inline-size: 100%;
    block-size: 100%;
    object-fit: cover;
    object-position: center;
  }

  @media (min-width: 64rem) {
    .guest-layout {
      grid-template-columns: 7fr 5fr;
      gap: var(--column-gap-desktop);
    }
  }
</style>
