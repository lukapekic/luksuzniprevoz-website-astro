/** Shared closing-conversion image assignment; no page-specific model claim. */
import { shared } from "./media/shared";
const finalCtaInterior = shared.interiors.finalCtaInterior;

export const closingMedia = { finalCtaInterior } as const;
